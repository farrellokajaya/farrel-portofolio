# The first version of this portfolio does not require environment variables.

# This variable will be used later for canonical URLs,
# Open Graph metadata, sitemap, and production configuration.
NEXT_PUBLIC_SITE_URL=http://localhost:3000